# Chạy dịch vụ này trên cổng 8083
server.port=8083

# --- Cấu hình kết nối MySQL (CSDL mới) ---
spring.datasource.url=jdbc:mysql://localhost:3306/guhastore_orders_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=123456

# --- Cấu hình JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- Cấu hình JWT (RẤT QUAN TRỌNG) ---
# CHUỖI BÍ MẬT NÀY PHẢI GIỐNG HỆT CHUỖI BÍ MẬT TRONG 'service-users'
jwt.secret=abcdefghijklmnopqrstuvwxyz123456